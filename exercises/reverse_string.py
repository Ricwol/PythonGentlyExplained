"""Exercise #36: Reverse String

Write a `reverseString()` function with a `text` parameter.
The function should return a string with all of text's characters in
reverse order.

For example, `reverseString('Hello')` returns `'olleH'`. 

The function should not alter the casing of any letters. And, if `text`
is a blank string, the function returns a blank string.
"""

def reverseString(text: str) -> str:
    """
    Return a string with all of `text`'s characters in reverse order.
    """
    return ''
