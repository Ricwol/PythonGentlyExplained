"""Exercise #22: Rock, Paper, Scissors

Write a `rpsWinner()` function with parameters `player1` and `player2`.
These parameters are passed one of the strings `'rock'`, `'paper'`, or
`'scissors'` representing that player's move. 

If this results in player 1 winning, the function returns `'player one'`.
If this results in player 2 winning, the function returns `'player two'`.
Otherwise, the function returns `'tie'`.
"""

def rpsWinner(player1: str, player2: str) -> str:
    """
    Return winner of a game of Rock, Paper, Scissors. 'tie' otherwise.
    """
    return ''
