"""Exercise #20: Leap Year

Write a `isLeapYear()` function with an integer `year` parameter.
If `year` is a leap year, the function returns `True`. Otherwise, 
the function returns `False`.
"""

def isLeapYear(year: int) -> bool:
    """Return True if `year` is leap year. False otherwise."""
    return False
