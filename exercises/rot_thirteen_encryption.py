"""Exercise #41: ROT 13 Encryption

Write a `rot13()` function with a `text` parameter that returns the
ROT 13 encrypted version of `text`.
Uppercase letters encrypt to uppercase letters and lowercase letters
encrypt to lowercase letters.

For example, `'HELLO, world!'` encrypts to `'URYYB, jbeyq!'` and
`'hello, WORLD!'` encrypts to `'uryyb, JBEYQ!'`.

You may use the following Python functions and string methods as part
of your solution: `ord()`, `chr()`, `isalpha()`, `islower()`, and `isupper()`.
"""

def rot13(text: str) -> str:
    """Return ROT 13 encrypted version of `text`."""
    return ''
